defaults:
  - _self_
  - model: baseline

data:
  seq_len: 256
  num_tokens: 10000000
  batch_size: 16
  num_workers: 4
  pin_memory: true
train:
  lr: 1e-3 
  epochs: 2
  log_interval: 200
  eval_iterations: 200
  use_wandb: true
  use_fp16: true